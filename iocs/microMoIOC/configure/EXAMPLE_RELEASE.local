# Use the following lines if motorMicroMo was built inside motor
MOTOR=
-include $(MOTOR)/configure/RELEASE
# Use the following lines if motorMicroMo was built outside motor
#!MOTOR_MICROMO=
#!-include $(MOTOR_MICROMO)/configure/RELEASE.local
